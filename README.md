# WoW Service Armory — Frontend

Vue 3 + TypeScript character and guild lookup UI for World of Warcraft.

## Stack

Vue 3 (`<script setup>` + TS), Vite, Pinia, vue-router, TanStack Query, Tailwind CSS, DaisyUI (base tokens only).

## Quick start

```bash
cp .env.example .env
npm install
npm run dev        # Vite on port 5173
npm run build      # type-check + production build → dist/
```

## Docs

- `CLAUDE.md` — architecture, conventions, component guide (primary LLM reference)
- `docs/design-guide.md` — full design system: colors, typography, card patterns, CSS tokens
